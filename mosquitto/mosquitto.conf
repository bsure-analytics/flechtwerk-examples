# Demo broker for the MQTT bridge examples. Anonymous access keeps the
# example wiring minimal; persistence keeps the QoS-1 session backlog across
# a broker restart, which is what the MqttExtractor's clean_session=False
# story relies on.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
